import React from 'react';
import './hero.scss'

const Hero = () => {
    return (
        <hero id="hero">
            <div className="container">
                <div className="hero--general">

                </div>
            </div>
        </hero>
    );
};

export default Hero;